package gable

{% for import in imports %}import "{{ import }}"
{% endfor %}
// {{CLASS_NAME}} 
type {{CLASS_NAME}} struct {
	{% for field in fields %}// {{field.field_desc}}
	{{ field.field_name }} {{ field.field_type }} `json:"{{ field.field_name }}"`
	{% endfor %}
}